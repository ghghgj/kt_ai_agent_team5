# analyzer.py — 팀원 B 담당
# 역할: 지식 그래프 데이터를 받아 투자 브리핑 리포트 생성 (Agent 2)
# ⚠️ run_agent2_analyzer 함수의 내부 로직만 교체하면 됩니다. 함수 시그니처는 절대 변경 금지.


def run_agent2_analyzer(graph_data: dict) -> str:
    """
    지식 그래프 딕셔너리를 분석하여 마크다운 형식의 투자 브리핑 리포트를 반환합니다.

    Args:
        graph_data (dict): extractor.py의 run_agent1_extractor()가 반환한 딕셔너리.
                           {"nodes": [...], "edges": [...], "metadata": {...}} 구조.

    TODO (팀원 B):
        - graph_data의 nodes·edges를 프롬프트로 변환하여 LLM 호출
        - 페르소나: "냉철하고 논리적인 시니어 퀀트 애널리스트"
        - 반환값은 반드시 마크다운 형식의 str 유지

    Returns:
        str: 마크다운 형식의 투자 브리핑 리포트
    """
    # ── 더미 데이터 (실제 구현 전까지 이 블록을 사용) ──────────────────────
    source_date = graph_data.get("metadata", {}).get("source_date", "N/A")
    node_count = len(graph_data.get("nodes", []))
    edge_count = len(graph_data.get("edges", []))

    dummy_report = f"""
# 📊 AI 투자 브리핑 리포트
**분석일:** {source_date} | **분석 엔티티:** {node_count}개 | **관계:** {edge_count}개

---

## 1. 오늘의 핵심 시그널

| 종목 | 이벤트 | 방향성 | 신뢰도 |
|------|--------|--------|--------|
| 삼성전자 | 3나노 수율 90% 돌파 → HBM4 양산 | ▲ 강력 매수 | 92% |
| SK하이닉스 | CHIPS Act 보조금 1.2조 + 외국인 순매수 | ▲ 매수 | 88% |
| 카카오 | 카카오브레인 흑자 전환 | ▲ 중립→매수 | 75% |

---

## 2. 인과관계 분석 (지식 그래프 경로)

```
미국 CHIPS Act
    └─[CATALYZES]→ SK하이닉스 (인디애나 공장)
                       └─[ATTRACTS]→ 외국인 투자자 순매수 ▲

삼성전자 (3나노 수율 90%)
    └─[PRODUCES]→ HBM4
                   └─[SUPPLIES_TO]→ 엔비디아 공급 협상 재개 ▲
```

---

## 3. 투자 전략 제안

### 🔴 단기 (1~2주)
- **SK하이닉스 비중 확대**: 외국인 수급 + 정책 모멘텀 동시 작동. 목표가 **230,000원**.

### 🟡 중기 (1~3개월)
- **삼성전자 분할 매수**: HBM4 양산 본격화 시 엔비디아 수주 가시화. 목표가 **85,000원**.

### 🟢 장기 관찰
- **카카오**: 브레인 흑자 전환은 긍정적이나 플랫폼 규제 리스크 잔존. 추가 확인 필요.

---

## 4. 리스크 요인

- 미·중 반도체 추가 수출 규제 가능성
- 원/달러 환율 급등 시 외국인 수급 이탈 위험
- 엔비디아 실적 쇼크 시 HBM 수요 둔화

---
> ⚠️ 본 리포트는 AI가 생성한 참고용 자료이며, 투자 결정의 최종 책임은 투자자 본인에게 있습니다.
""".strip()

    return dummy_report
    # ── 더미 데이터 끝 ───────────────────────────────────────────────────────
